# encoding: UTF-8
# vim: ts=2 sts=2 sw=2 expandtab

require_relative 'entry'

module Crontab
  class Schedule
    # TODO:
    #   * list of ENV settings/vars
    #   * list of Entry objects
    #   * Schedule.from(str) / Schedule.from_file(fh) / ?
  end
end

